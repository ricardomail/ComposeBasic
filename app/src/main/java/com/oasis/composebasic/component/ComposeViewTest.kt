package com.oasis.composebasic.component

/**
 * xml使用compose view
 * 使用ComposeView组件， 创建一个id
 * 然后使用findviewById拿到这个view
 * composeView.setContent{
 *
 * } 来使用
 *
 *
 * compose使用xml
 * 使用AndroidView(factory = {
 *  TextView(it)
 * }){tv ->
 *      // 进行view的操作
 * }
 */